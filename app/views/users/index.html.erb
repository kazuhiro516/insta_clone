<div class="container">
  <h1 class="text-center">全てのユーザー</h1>
    <ul class="users">
      <% @users.each do |user| %>
        <li>
          <%= image_tag avatar_url(user) %>
          <%= link_to user.name, user %>
        </li>
      <% end %>
    </ul>
</div>